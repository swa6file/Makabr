<Window x:Class="ConstructionSiteWPF.Views.WorkerEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="400" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding Title}"
                   FontSize="18"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 20"/>

        <!-- Форма редактирования -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Имя -->
            <TextBlock Grid.Row="0" Grid.Column="0" 
                       Text="Имя:" 
                       Margin="0 0 10 10"
                       VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" 
                     Text="{Binding Worker.Name, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 10"
                     Height="25"/>

            <!-- Возраст -->
            <TextBlock Grid.Row="1" Grid.Column="0" 
                       Text="Возраст:" 
                       Margin="0 0 10 10"
                       VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding Worker.Age, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 10"
                     Height="25"/>

            <!-- Зарплата -->
            <TextBlock Grid.Row="2" Grid.Column="0" 
                       Text="Зарплата:" 
                       Margin="0 0 10 10"
                       VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" 
                     Text="{Binding Worker.Salary, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 10"
                     Height="25"/>

            <!-- Специализация -->
            <TextBlock Grid.Row="3" Grid.Column="0" 
                       Text="Специализация:" 
                       Margin="0 0 10 10"
                       VerticalAlignment="Center"/>
            <ComboBox Grid.Row="3" Grid.Column="1" 
                  ItemsSource="{Binding Specializations}"
                  SelectedItem="{Binding Worker.Specialization, UpdateSourceTrigger=PropertyChanged}"
                  Margin="0 0 0 10"
                  Height="25"/>

        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0">
            <Button Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Width="100"
                    Margin="0 0 10 0"/>
            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>